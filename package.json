{
  "name": "bera-youtube-api",
  "version": "3.0.0",
  "description": "Bera YouTube API - Working Version",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install-deps": "npm install && curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o yt-dlp && chmod +x yt-dlp",
    "test": "node -e \"import('./server.js').then(() => console.log('Server loaded'))\""
  },
  "dependencies": {
    "express": "^5.0.1",
    "express-rate-limit": "^8.2.1",
    "helmet": "^7.1.0",
    "cors": "^2.8.5",
    "fluent-ffmpeg": "^2.1.3",
    "play-dl": "^1.9.7",
    "yt-dlp-exec": "^2.2.4",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
